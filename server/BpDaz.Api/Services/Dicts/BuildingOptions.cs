namespace BpDaz.Api.Services.Dicts;

/// <summary>
/// Какие здания предлагать в форме заявки. В BpDazApp они были зашиты прямо в
/// разметку AddRequest.cshtml тремя option-ами (130 Блок Б, 131 Мажилис ПРК,
/// 132 Сенат ПРК) — здесь тот же список вынесен в конфигурацию, потому что
/// идентификаторы у каждой базы свои.
/// </summary>
public class BuildingOptions
{
    /// <summary>
    /// Коды разрешённых зданий в порядке показа. Пустой список снимает
    /// ограничение: тогда отдаются все площадки верхнего уровня из Places.
    /// </summary>
    public int[] Ids { get; set; } = [];
}
